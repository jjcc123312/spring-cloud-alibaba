spring:
  cloud:
    nacos:
    # 注册进 nacos
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: 78af34d1-e0d2-42c6-ac4c-af049c19efd5 # 开发环境
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml   #nacos配置文件后缀，下文配置发布的时候会用到。注意是yaml，不是yml
        namespace: 78af34d1-e0d2-42c6-ac4c-af049c19efd5 # 开发环境空间
        shared-configs:
          - dataId: ${spring.application.name}.yaml # 文件Id
            group: DEFAULT_GROUP # 分组
            refresh: true     # 是否自动刷新
    gateway:
      discovery:
        locator:
          # 注册到注册中心
          enabled: true
          url-expression: "'lb://' + serviceId" # 路由的目标地址的表达式，默认为 "'lb://' + serviceId"
#      routes:
#        - id: test # 路由的编号
#          uri: http://www.iocoder.cn # 路由到的目标地址
#          predicates:
#            - Path=/blog # 断言，作为路由的匹配条件，对应 RouteDefinition 数组
#          filters:
#            - StripPrefix=1 # 过滤器，对请求进行拦截，实现自定义的功能，对应 FilterDefinition 数组
#        - id: oschina
#          uri: https://www.oschina.net
#          predicates:
#            - Path=/oschina
#          filters:
#            - StripPrefix=1 # 对应的具体实现是 `StripPrefixGatewayFilterFactory`，可以接收一个非负整数，作用是去掉url的层数，整数即对应层数。
